import * as React from "react";
import Button from "@mui/material/Button";
import Stack from "@mui/material/Stack";
export default function BotonLog(props) {
  return (
    <Stack spacing={2} direction="row">
      <Button
        variant="text"
        size="small"
        onClick={this.props.first}
        Click
        me
      >
        Log in
      </Button>
    </Stack>
  );
}
